define([
        'jquery',
        'configuration',
        'underscore',
        'backbone',
    'i18n!app/nls/entities'
], function ($, config, _, Backbone, entities_strings) {
    
    var BaseView = Backbone.View.extend({
    
        

    });

    return BaseView;
});
